// daqui pra frente utiliza-se node com saida no terminal
// a saida de console utilizada no browser não funciona para JS. 

var vel = 72.4
if (vel > 60)
    console.log("Você ultrassou a velocidade permitida, a MULTA vai pra sua casa")
else 
console.log(`A velocidade do seu carro é ${vel} km por hora, continue a sua viagem`)


